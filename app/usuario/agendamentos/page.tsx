import { ModuleIndex } from './_components';

// page.tsx (sem "use client")
export const metadata = {
	title: 'DrClick - Meus Agendamentos',
};

export default function Page() {
	return <ModuleIndex />;
}