import { ModuleIndex } from './_components';

// page.tsx (sem "use client")
export const metadata = {
	title: 'DrClick - Exames Disponíveis',
};

export default function Page() {
	return <ModuleIndex />;
}